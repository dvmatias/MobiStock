package com.samuraicmdv.featurebarcodescanner.event

/**
 * BarcodeScannerNavigationEvent is a sealed interface that represents the navigation events that can be emitted by the
 * BarcodeScanner flow.
 */
sealed interface BarcodeScannerNavigationEvent : BarcodeScannerEvent